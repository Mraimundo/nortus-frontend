import { TicketLayout } from '@/src/modules/tickets/components/TicketLayout';
import { TicketContent } from '@/src/modules/tickets/components/TicketContent';

export default function TicketsManegementPage() {
  return (
    <TicketLayout>
      <TicketContent />
    </TicketLayout>
  );
}
